<div class="max-w-4xl mx-auto px-4 py-8">
    {{-- Header --}}
    <div class="mb-8">
        <h1 class="text-3xl font-black text-gray-800 mb-2">Quản lý danh mục 🏷️</h1>
        <p class="text-gray-600">Tạo và quản lý danh mục cho ghi chú</p>
    </div>

    {{-- Thông báo --}}
    @if (session()->has('message'))
        <div class="mb-6 p-4 bg-green-500 text-white rounded-2xl shadow-lg animate-pulse">
            {{ session('message') }}
        </div>
    @endif
    @if (session()->has('error'))
        <div class="mb-6 p-4 bg-red-500 text-white rounded-2xl shadow-lg animate-pulse">
            {{ session('error') }}
        </div>
    @endif

    {{-- Form tạo mới --}}
    <div class="bg-white p-8 rounded-3xl shadow-xl border border-gray-100 mb-8">
        <h2 class="text-xl font-bold text-gray-800 mb-6">➕ Tạo danh mục mới</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Tên danh mục:</label>
                <input wire:model.live="name" type="text" 
                       class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition-all"
                       placeholder="VD: Công việc, Cá nhân...">
                @error('name') 
                    <span class="text-red-500 text-xs mt-1 block">{{ $message }}</span> 
                @enderror
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Màu sắc (tùy chọn):</label>
                <select wire:model="color" 
                        class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition-all">
                    <option value="">Mặc định (xám)</option>
                    <option value="blue">🔵 Xanh dương</option>
                    <option value="green">🟢 Xanh lá</option>
                    <option value="purple">🟣 Tím</option>
                    <option value="pink">🌸 Hồng</option>
                    <option value="yellow">🟡 Vàng</option>
                    <option value="red">🔴 Đỏ</option>
                    <option value="indigo">🟣 Chàm</option>
                    <option value="gray">⚫ Xám</option>
                </select>
                @error('color') 
                    <span class="text-red-500 text-xs mt-1 block">{{ $message }}</span> 
                @enderror
            </div>
        </div>

        <button wire:click="store" wire:loading.attr="disabled"
                class="mt-8 w-full py-4 bg-blue-500 hover:bg-blue-600 disabled:bg-blue-300 text-white font-bold rounded-2xl transition-all shadow-lg shadow-blue-100 disabled:cursor-not-allowed flex items-center justify-center">
            <span wire:loading.remove>TẠO DANH MỤC MỚI</span>
            <span wire:loading>Đang tạo...</span>
        </button>
    </div>

    {{-- Danh sách danh mục --}}
    <div class="bg-white rounded-3xl shadow-xl border border-gray-100 overflow-hidden">
        <div class="p-8 border-b border-gray-100">
            <h3 class="text-xl font-bold text-gray-800 mb-2">📋 Danh sách danh mục</h3>
            <p class="text-sm text-gray-600">Tổng: <span class="font-bold text-blue-600">{{ $categories->count() }}</span> danh mục</p>
        </div>

        <div class="divide-y divide-gray-100">
            @forelse($categories as $category)
                <div class="p-6 hover:bg-gray-50 transition-colors">
                    <div class="flex justify-between items-center">
                        <div class="flex items-center space-x-4">
                            <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-{{ $category->color }}-400 to-{{ $category->color }}-500 flex items-center justify-center">
                                <span class="font-bold text-white text-sm">{{ substr($category->name, 0, 2) }}</span>
                            </div>
                            <div>
                                <h4 class="font-bold text-gray-800">{{ $category->name }}</h4>
                                <p class="text-sm text-gray-500">ID: {{ $category->id }} | Màu: {{ $category->color ?? 'gray' }}</p>
                            </div>
                        </div>
                        
                        <div class="flex items-center space-x-2">
                            <span class="text-xs bg-green-100 text-green-800 px-3 py-1 rounded-full font-bold">
                                {{ $category->notes()->count() }} ghi chú
                            </span>
                            @if($category->notes()->count() == 0)
                                <button wire:click="delete({{ $category->id }})" 
                                        wire:confirm="Xóa danh mục '{{ $category->name }}'? Chỉ xóa khi không có ghi chú."
                                        class="p-2 text-red-500 hover:bg-red-50 rounded-xl transition-all">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                    </svg>
                                </button>
                            @endif
                        </div>
                    </div>
                </div>
            @empty
                <div class="text-center py-12 text-gray-500">
                    <p class="text-lg font-medium mb-2">Chưa có danh mục</p>
                    <p class="text-sm">Tạo danh mục đầu tiên ở trên!</p>
                </div>
            @endforelse
        </div>
    </div>

    {{-- Link về NoteManager --}}
    <div class="mt-8 text-center">
        <a href="{{ route('notes.index') }}" class="inline-flex items-center px-6 py-3 bg-orange-500 hover:bg-orange-600 text-white font-bold rounded-2xl transition-all shadow-lg">
            ← Quay lại Kanban Board
        </a>
    </div>
</div>